module.exports = (req, res) => {
  res.status(200).send("CRM project root — use /api/customer");
};

module.exports = (req, res) => {
  res.status(200).send("CRM project root — use /api/customer");
};
